#!/bin/bash

cur_file=$(readlink -f $BASH_SOURCE)
cur_dir=$(dirname $cur_file)
conf_file=$cur_dir/__local/jop.conf
temp_dir=$TMPDIR
notes_dir=

for f in `ls $cur_dir/src/*.sh`; do
    source $f
done

main $*
